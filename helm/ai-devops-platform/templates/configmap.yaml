apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-devops-platform.fullname" . }}-config
  labels:
    {{- include "ai-devops-platform.labels" . | nindent 4 }}
data:
  APP_ENV: {{ .Values.appConfig.environment | quote }}
  LOG_LEVEL: {{ .Values.appConfig.logLevel | quote }}