apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-devops-platform.fullname" . }}
  labels:
    {{- include "ai-devops-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-devops-platform.selectorLabels" . | nindent 4 }}
